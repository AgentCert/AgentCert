agent_id, agentID fallsback to environment id if expreiment manifest has no agent id details. Need to rework on this after sanjeev changes are pushed
data contracts needs to be provided by Ravi. After this latest changes, UI integration needs to be updated
remove whereever there is stubId

Remove this line frm useexperimentcompletiontoast.tsx
 if (!response.ok) {
    // TODO: Remove this stub once the bucketing-extraction API is live
    console.warn(`Bucketing-extraction API unavailable (${response.status}), using stub response`);
    const stubId = `stub-${experimentId}-${Date.now()}`;
    return {
      status: 'accepted',
      task_id: stubId,
      poll_url: `/api/v1/tasks/${stubId}`
    };
  }

Remove Generate Certificate feature and all corresponding logic:
- views/ExperimentRunHistory/ExperimentRunHistoryTable.tsx: remove hidden {false && (...)} MenuItem block, remove openCertDialog/closeCertDialog/isCertDialogOpen if unused, remove CertificateReportDialog import if unused
- views/ExperimentRunDetails/ExperimentRunDetailsHeader.tsx: remove hidden {false && ...} Text block, remove openCertDialog/closeCertDialog/isCertDialogOpen if unused, remove CertificateReportDialog import and JSX if unused
- components/CertificationProgressDialog/CertificationProgressDialog.tsx: remove entire component
- strings/strings.en.yaml: remove `generateCertificate: Generate Certificate`
- strings/types.ts: remove `'generateCertificate': unknown`
- Search for any other files importing CertificateReportDialog or CertificationProgressDialog and remove those
- Remove any backend API routes/handlers for POST aggregation-certification and GET cert-tasks