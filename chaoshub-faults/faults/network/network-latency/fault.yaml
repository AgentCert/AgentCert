---
apiVersion: litmuschaos.io/v1alpha1
description:
  message: |
    Injects network latency on the target pods using traffic control (tc) and iptables.
    This fault delays outgoing traffic from the target pod(s) to simulate high-latency conditions.
kind: ChaosExperiment
metadata:
  name: network-latency
  labels:
    name: network-latency
    app.kubernetes.io/part-of: litmus
    app.kubernetes.io/component: chaosexperiment
    app.kubernetes.io/version: ci
spec:
  definition:
    scope: Namespaced
    permissions:
      # Create and monitor the experiment & helper pods
      - apiGroups: [""]
        resources: ["pods"]
        verbs:
          [
            "create",
            "delete",
            "get",
            "list",
            "patch",
            "update",
            "deletecollection",
          ]
      # Performs CRUD operations on the events inside chaosengine and chaosresult
      - apiGroups: [""]
        resources: ["events"]
        verbs: ["create", "get", "list", "patch", "update"]
      # Fetch configmaps details and mount it to the experiment pod (if specified)
      - apiGroups: [""]
        resources: ["configmaps"]
        verbs: ["get", "list"]
      # Track and get the runner, experiment, and helper pods log
      - apiGroups: [""]
        resources: ["pods/log"]
        verbs: ["get", "list", "watch"]
      # for creating and managing to execute commands inside target container
      - apiGroups: [""]
        resources: ["pods/exec"]
        verbs: ["get", "list", "create"]
      # deriving the parent/owner details of the pod(if parent is anyof {deployment, statefulset, daemonsets})
      - apiGroups: ["apps"]
        resources: ["deployments", "statefulsets", "replicasets", "daemonsets"]
        verbs: ["list", "get"]
      # deriving the parent/owner details of the pod(if parent is deploymentConfig)
      - apiGroups: ["apps.openshift.io"]
        resources: ["deploymentconfigs"]
        verbs: ["list", "get"]
      # deriving the parent/owner details of the pod(if parent is deploymentConfig)
      - apiGroups: [""]
        resources: ["replicationcontrollers"]
        verbs: ["get", "list"]
      # deriving the parent/owner details of the pod(if parent is argo-rollouts)
      - apiGroups: ["argoproj.io"]
        resources: ["rollouts"]
        verbs: ["list", "get"]
      # for configuring and monitor the experiment job by the chaos-runner pod
      - apiGroups: ["batch"]
        resources: ["jobs"]
        verbs: ["create", "list", "get", "delete", "deletecollection"]
      # for creation, status polling and deletion of litmus chaos resources used within a chaos workflow
      - apiGroups: ["litmuschaos.io"]
        resources: ["chaosengines", "chaosexperiments", "chaosresults"]
        verbs: ["create", "list", "get", "patch", "update", "delete"]
    image: "litmuschaos.docker.scarf.sh/litmuschaos/go-runner:latest"
    imagePullPolicy: Always
    args:
      - -c
      - ./experiments -name network-latency
    command:
      - /bin/bash
    env:
      - name: TOTAL_CHAOS_DURATION
        value: "60"

      # Period to wait before and after injection of chaos in sec
      - name: RAMP_TIME
        value: ""

      # Network latency to be injected in milliseconds
      - name: NETWORK_LATENCY
        value: "100"

      # Jitter in milliseconds
      - name: JITTER
        value: "0"

      # Network interface to use for traffic control
      - name: NETWORK_INTERFACE
        value: "eth0"

      # Destination IP/CIDR for which latency is to be injected
      - name: DESTINATION_NETWORK
        value: ""

      # Source port for which latency is to be injected
      - name: SOURCE_PORT
        value: ""

      # Destination port for which latency is to be injected
      - name: DESTINATION_PORT
        value: ""

      # percentage of total pods to target
      - name: PODS_AFFECTED_PERC
        value: ""

      - name: TARGET_CONTAINER
        value: ""

      - name: TARGET_PODS
        value: ""

      # Protocol to target (tcp/udp/icmp)
      - name: PROTOCOL
        value: "tcp"

      # Sequence of chaos injection
      - name: SEQUENCE
        value: "parallel"

